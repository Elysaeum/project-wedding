import { ServicePricePipe } from './service-price.pipe';

describe('ServicePricePipe', () => {
  it('create an instance', () => {
    const pipe = new ServicePricePipe();
    expect(pipe).toBeTruthy();
  });
});
